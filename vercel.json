{
  "buildCommand": "npx pnpm --filter shared build && npx pnpm --filter db build && npx pnpm --filter templates build && npx pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
